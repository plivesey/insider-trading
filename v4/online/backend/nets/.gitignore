# Training scratch — keep only the shipped artifacts in version control.
# Production (serverState.ts) requires champion.json + bot_params.json.
*_log.csv
champion_selfplay.json
champion_seed.json
champion_round*.json
latest.json
